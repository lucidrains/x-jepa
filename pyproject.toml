[project]
name = "x-jepa"
version = "0.0.100"
description = "Explorations into JEPA"
authors = [
    { name = "Phil Wang", email = "lucidrains@gmail.com" }
]
readme = "README.md"
requires-python = ">= 3.10"
license = { file = "LICENSE" }
keywords = [
    'artificial intelligence',
    'deep learning',
    'latent space',
    'jepa',
    'world model'
]

classifiers=[
    'Development Status :: 4 - Beta',
    'Intended Audience :: Developers',
    'Topic :: Scientific/Engineering :: Artificial Intelligence',
    'License :: OSI Approved :: MIT License',
    'Programming Language :: Python :: 3.10',
]

dependencies = [
    "assoc-scan>=0.0.5",
    "einops>=0.8.1",
    "einx>=0.3.0",
    "ema-pytorch>=0.8.3",
    "env-ssl-wrapper==0.1.1",
    "loguru",
    "memmap-replay-buffer>=0.1.10",
    "PoPE-pytorch",
    "torch>=2.4",
    "torch-einops-utils>=0.1.11",
    "x-mlps-pytorch"
]

[project.urls]
Homepage = "https://pypi.org/project/x-jepa/"
Repository = "https://codeberg.org/lucidrains/x-jepa"

[project.optional-dependencies]
examples = []
test = [
    "pytest",
    "gymnasium",
    "populora>=0.1.12"
]

[tool.pytest.ini_options]
pythonpath = [
    "."
]
testpaths = [
    "tests"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build]
include = [
    "x_jepa",
    "pyproject.toml",
    "README.md",
    "LICENSE"
]

[tool.hatch.build.targets.wheel]
packages = ["x_jepa"]
